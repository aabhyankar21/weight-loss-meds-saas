# Weight Loss Meds Lander

Forbes Health–style GLP-1 listicle landing page, built to match the Figma desktop and mobile frames.

```bash
npm install
npm run dev
```

Open [http://localhost:5173](http://localhost:5173).
